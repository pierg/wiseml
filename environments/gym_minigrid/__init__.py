# Import the envs module so that envs register themselves
import gym_minigrid.envs

# Import minigrid and extendedminigrid so it's accessible when installing with pip
import gym_minigrid.extendedminigrid
import gym_minigrid.minigrid